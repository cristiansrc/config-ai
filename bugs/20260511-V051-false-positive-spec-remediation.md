# Spec Remediator Bug Report — V051 False-Positive @JdbcTypeCode Mismatch

**Date:** 2026-05-11
**Project:** ms-ticket-user (Increment 01)
**Active repo path:** `/home/cristiansrc/Documentos/Proyectos/ms-ticket-user`
**Finding ID:** V051
**Severity reported by Validator:** High
**Classification:** validator/process-bug

---

## Finding Text
> **V051 — PostgreSQL array JPA mapping instruction is inconsistent between spec and task board**
>
> Evidence: The increment spec says `@JdbcTypeCode(SqlType.ARRAY)` (singular `SqlType`), while the task board instructs `@JdbcTypeCode(Types.ARRAY)`.

## Actual State of Artifacts (at time of remediation, 2026-05-11)
Both artifacts use **identical** constant: `Types.ARRAY`
- Spec §5 line 156: `` - `roles` stored as PostgreSQL array (`String[]` with `@JdbcTypeCode(Types.ARRAY)`). ``
- Task board S-003 lines 72 & 76: `@JdbcTypeCode(Types.ARRAY)` (both occurrences)

The spec previously used `SqlType.ARRAY` in earlier cycles, but was corrected to `Types.ARRAY` during remediation cycle V046. The task board has always used `Types.ARRAY`. Both are now aligned.

## Why the Finding Could Not Be Resolved
The finding is a **false-positive** generated by the Spec Validator. The validator appears to be reading stale or cached content from the spec, reporting `SqlType.ARRAY` when the actual file contains `Types.ARRAY`. No remediation was possible because there is no actual drift between artifacts.

## Suspected Root Cause
- **Agent prompt / model behavior:** The validator may have read an outdated version of the increment spec during its validation run (possibly from a cached state before V046 correction). This could be due to:
  - Compaction memory not being refreshed before validation
  - Model hallucination of `SqlType.ARRAY` when scanning the file
  - Validation tool using stale artifact cache instead of live disk read

## Why Spec Remediator Was Not Authorized To Edit
Not applicable. The finding was a false-positive; no edit was needed or attempted. Both artifacts already agreed on `Types.ARRAY`.

## Recommended Refactor for Agents/Skills/Process
1. **Validator must re-read artifacts from disk** before each validation run, especially after previous remediation cycles that modified those files.
2. **Add a guard in Spec Validator skill:** Before reporting contract-drift findings, verify the exact string appears in both files using line-level evidence. If one file does not contain the claimed string, flag as potential false-positive and require double-check.
3. **Consider adding artifact hash/version tracking** in shared context to detect when validator is reading stale versions vs current disk state.

## Next Safe Action
- Mark V051 as superseded/false_positive in remediation log (done).
- Proceed with remaining findings V048, V049, V050 which are legitimate blockers requiring Planner/User decisions.
